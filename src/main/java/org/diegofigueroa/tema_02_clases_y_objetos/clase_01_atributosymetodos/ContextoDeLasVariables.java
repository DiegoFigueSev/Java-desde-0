package org.diegofigueroa.tema_02_clases_y_objetos.clase_01_atributosymetodos;

public class ContextoDeLasVariables {
    {
        // * Esta variable solo vive en mi metodo dentor de mis llaves
        int number = 10;

        // * Variables locales; Solo viven dentro de las llaves, dentro de la funcion, metodo, clase
        int myVar = 24;

        // * Las cariables de clase son las que se definen dentro de una clase, son los atributos de la clase

        // * Las variables locales, existen unicamente durante la ejecucion del metodo donde se declaran
        // * Las variables de instancia existen mientras la instancia de la clase esten en memoria
        // * Las variables de clase existen siempre
    }
}
